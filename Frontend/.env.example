# Optional. Defaults to /api; Vite proxies that path to the required local backend.
VITE_API_URL=/api
# Browser key only: restrict it by HTTP referrer in Google Cloud Console.
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-browser-key
